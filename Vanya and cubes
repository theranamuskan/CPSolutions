#include <bits/stdc++.h>
using namespace std;
int n, h, t, cnt;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    while (cnt <= n)
    {
        h++;
        cnt += (h * (h + 1)) / 2;
    }
    cout << h - 1;

    return 0;
}
